1)Install Python 3.x
2)Download all the necessary packages
3)Add “SpamClassifier.py” and the dataset in the same directory. 
4)Run the program in the terminal by invoking the Python interpreter manually as follows :
python SpamClassifier.py
5)To test some other dataset, replace the test_data in the for loop where preds is getting calculated with the dataset’s name and it has to be in .csv format.